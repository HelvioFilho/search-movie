import styled from 'styled-components/native';

export const SearchSelect = styled.TouchableOpacity`
  width: 100%;
  padding: ${({ theme }) => theme.metrics.px(14)}px;
  margin-top: ${({ theme }) => theme.metrics.px(10)}px;
`;